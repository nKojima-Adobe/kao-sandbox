# Horizontal Line — Extraction Summary

## Overview
| Property | Value |
|----------|-------|
| Block name | `horizontal-line` |
| Block type | Standard self-contained |
| Source files | 3 (JS, CSS, JSON) |
| Total lines | ~56 |
| External dependencies | 0 |
| Placeholder types | 2 (`{BLOCK_NAME}`, `{BLOCK_FUNC}`) |

## Key Considerations
1. **Zero dependencies** — drop-in ready for any EDS project
2. **2 CSS custom properties** — `--spacer-04` (margin) and `--nec-gray-30` (line color)
3. **No stories file** — block is too simple for Storybook variants
4. **Empty model fields** — no author configuration, just a visual separator
