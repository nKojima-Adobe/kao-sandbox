# BLOCK IMPLEMENTATION INSTRUCTIONS (for Cursor AI)

You are Cursor AI.  
You are currently inside a block folder that was generated by the block extraction process and then manually placed into a **target AEMaaCS/EDS project**.

Your job is to **integrate this block into the target project**, using the extracted JS, CSS, JSON, and documentation that exist inside this folder.

This file is **fully interactive**.  
If you do not have enough information, you must **ask the user questions first** and wait for answers.  
After receiving answers, **restart the execution of this file from the beginning**.

**IMPORTANT — Web Component Architecture:**
This block uses a dual-structure pattern:
- **`components/{BLOCK_NAME}/`** — The actual Web Component (`{BLOCK_FUNC} extends HTMLElement`) and its CSS
- **`blocks/{BLOCK_NAME}/`** — No-op decorator files (empty `decorate()` and empty CSS)
- The Web Component is **imported and instantiated by a parent block** (typically the header)

---

# 0. ENVIRONMENT VALIDATION (MANDATORY)

Before you do anything, you must validate:

1. **That this file is located inside a `_block-export/` folder within a project**
2. **That the required extracted files/folders exist**
3. **That the surrounding project appears to be an AEM Edge Delivery Services (EDS) project**

If any of these checks fail → **STOP immediately**, explain the issue, and ask the user to correct it.

---

## 0.1 Validate folder location

Verify that:

- The current directory path includes:  
  `/_block-export/`  
  Example:  
  `/target-project/_block-export/Back-To-Top/implementation.md`

If `_block-export/` is not in the path:

> ERROR: This implementation.md file must be placed in a folder under `/_block-export/` inside the target project.  
> Current path appears incorrect.  
> Please place this extracted block folder inside your target repository and try again.

(Stop execution until user fixes the placement.)

---

## 0.2 Validate required extracted contents

Inside this folder, ensure the minimum required contents exist:

| Required File | Purpose |
|---------------|---------|
| `{BLOCK_NAME}.js` | Web Component implementation |
| `{BLOCK_NAME}.css` | Web Component styles |
| `{BLOCK_NAME}-noop.js` | No-op block decorator |
| `{BLOCK_NAME}-noop.css` | Empty block styles |
| `_{BLOCK_NAME}.json` | AEM content model |
| `naming-map.json` | Placeholder → original name mapping |
| `model-snippet.json` | Component registration snippet |
| `header-integration-snippet.js` | Header integration code |

For Back-To-Top, these resolve to:
- `back-to-top.js`, `back-to-top.css`
- `back-to-top-noop.js`, `back-to-top-noop.css`
- `_back-to-top.json`
- `naming-map.json`, `model-snippet.json`, `header-integration-snippet.js`

**If any required file is missing → STOP and ask the user to provide the complete block export.**

---

## 0.3 Validate that the target repository is an EDS project

Check the folder structure *outside* this `_block-export` directory.

Look for at least one of the following:

- A `/blocks/` directory  
- A `/models/` directory  
- A `/components/` directory  
- A `/styles/` or `/styles/blocks/` directory  
- EDS config files (any of the following):  
  - `fstab.yaml`  
  - `helix-config.yaml`  
  - `helix-query.yaml`  
  - `package.json` references to Adobe/Franklin/EDS tools

If **none** of these exist:

> ERROR: This repository does not appear to be an EDS project.  
> I was expecting to find directories such as `/blocks/`, `/models/`, or EDS config files.  
> Please confirm that this is an EDS project before continuing.

Stop and ask the user for clarification.

---

# 1. GATHER USER INPUT (MANDATORY)

Before performing any file operations, ask the user for:

> Before I start implementing this block into your project, I need a few details:
>
> **1. What should this block be named in this project?**
> - Provide: `TARGET_BLOCK_NAME` (e.g. `back-to-top`, `scroll-to-top`)
>
> **2. Which parent block will import and instantiate this component?**
> - Typically the header block. Provide the path (e.g. `blocks/header/header.js`)
>
> **3. Where is the parent block's CSS file?**
> - (e.g. `blocks/header/header.css`)
>
> **4. Does your project have a `components/` directory?**
> - If yes, the Web Component will be placed there
> - If no, we'll create one or place it in an alternative location
>
> **5. Does your project have an analytics/tracking system?**
> - For `trackNavigationScrollTop`; if no, we'll stub it
>
> **6. Does your project use component model files?**
> - (e.g. `component-definition.json`, `component-models.json`, `component-filters.json`)
> - If yes, provide paths. If no, we'll only use the block-level `_back-to-top.json`

Wait for the user's reply.

Store:

- `TARGET_BLOCK_NAME`
- `PARENT_BLOCK_JS_PATH`
- `PARENT_BLOCK_CSS_PATH`
- `COMPONENTS_DIR` (e.g. `components/`)
- `HAS_ANALYTICS`
- `HAS_COMPONENT_MODELS`

Repeat back to the user to confirm correctness before continuing.

If unclear → explain the issue and ask again.

---

# 2. READ EXTRACTION METADATA AND NAMING MAP (MANDATORY)

## 2.1 Read extraction metadata

If the file `extraction-information.md` exists:

- Read original block name (`SOURCE_BLOCK_NAME`)
- Read architecture notes (Web Component pattern)
- Read any notes about dependencies, naming, or custom folder structures
- Use it to guide renaming and conflict resolution

If the file does not exist, proceed without it but mention this in the final summary.

## 2.2 Read naming map (CRITICAL)

The file `naming-map.json` is **MANDATORY** and contains:
- The placeholder scheme used during extraction
- All patterns that need to be replaced
- Original naming conventions
- The `componentType: "web-component"` indicator

**Read `naming-map.json` and understand:**

1. **Block type:** single block, Web Component architecture

2. **Placeholder patterns used:**
   - `{BLOCK_NAME}` — kebab-case (file names, custom element tag, data attributes)
   - `{BLOCK_CLASS}` — kebab-case (CSS classes)
   - `{BLOCK_FUNC}` — PascalCase (class name)
   - `{BLOCK_VAR}` — camelCase (variable names)

3. **What needs to be replaced:**
   - File names containing placeholders
   - Custom element tag in CSS selectors (e.g., `back-to-top { ... }`)
   - Custom element tag in `customElements.define()`
   - JavaScript class name
   - CSS class names (e.g., `.back-to-top-button`)
   - `querySelector` strings
   - JSON model IDs

If `naming-map.json` is missing:
> ERROR: The `naming-map.json` file is missing.  
> This file is required to properly rename the block during implementation.  
> The extraction may have been incomplete. Please re-extract the block or create the naming map manually.

Stop execution until resolved.

---

# 3. DERIVE TARGET NAMES FROM USER INPUT (MANDATORY)

Based on the user's input from Step 1, automatically derive all necessary naming variations:

If user provided: `TARGET_BLOCK_NAME = "back-to-top"`

Automatically generate:
- **File/folder name (kebab-case):** `back-to-top`
- **CSS class prefix (kebab-case):** `back-to-top`
- **Custom element tag (kebab-case):** `back-to-top`
- **Class name (PascalCase):** `BackToTop`
- **Variable name (camelCase):** `backToTop`

Display the derived names and ask for confirmation:

> I will use these names for your block:
>
> **Custom element tag:** `<back-to-top>`
> **File names:** `back-to-top.js`, `back-to-top.css`
> **JS class:** `class BackToTop extends HTMLElement`
> **CSS selectors:** `back-to-top { ... }`, `.back-to-top-button`
> **JS variables:** `backToTop`, `backToTopBlock`
>
> If this looks correct, I will proceed with the replacement.

Wait for user confirmation before proceeding.

---

# 4. REPLACE ALL PLACEHOLDERS IN EXTRACTED FILES (CRITICAL)

**Before copying any files to the target project, replace all placeholders:**

## 4.1 Create replacement mapping

From the user-confirmed names, create a replacement map:

```javascript
{
  "{BLOCK_NAME}": "back-to-top",
  "{BLOCK_CLASS}": "back-to-top",
  "{BLOCK_FUNC}": "BackToTop",
  "{BLOCK_VAR}": "backToTop",
  "{BLOCK_UPPER}": "BACK_TO_TOP"
}
```

## 4.2 Replace in file names

Rename files containing placeholders:
- `{BLOCK_NAME}.js` → `back-to-top.js`
- `{BLOCK_NAME}.css` → `back-to-top.css`
- `{BLOCK_NAME}-noop.js` → `back-to-top-noop.js`
- `{BLOCK_NAME}-noop.css` → `back-to-top-noop.css`
- `_{BLOCK_NAME}.json` → `_back-to-top.json`

## 4.3 Replace in file contents

For each file (JS, CSS, JSON):

1. **Read the entire file content**
2. **Apply all placeholder replacements** using the mapping from 4.1
3. **Write the updated content back**

**Special attention for this Web Component block:**
- `{BLOCK_NAME}` in CSS is used as an **element selector** (not a class), e.g., `back-to-top { ... }`
- `{BLOCK_FUNC}` is the **class name**, not a function name
- `customElements.define('{BLOCK_NAME}', {BLOCK_FUNC})` must have matching tag and class

## 4.4 Validate replacements

After all replacements:

1. **Check for remaining placeholders:**
   - Search all files for any remaining `{BLOCK_` patterns
   - If found, report them to the user

2. **Verify syntax:**
   - Ensure JS files still have valid syntax
   - Ensure CSS files still have valid syntax
   - Verify `customElements.define()` call is correct

3. **Log replacements:**
   - Count total replacements made
   - List files that were modified

If validation fails:
> ERROR: Placeholder replacement failed validation.
> - Files with remaining placeholders: [list]
> - Syntax errors found: [list]

Stop and ask user how to proceed.

---

# 5. COPY JS/TS FILES INTO THE TARGET PROJECT

This block requires files in **two** locations:

## 5.1 Web Component files → `components/`

Copy the main Web Component files to the components directory:

```bash
mkdir -p components/TARGET_BLOCK_NAME/
```

Copy:
- `back-to-top.js` → `components/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.js`
- `back-to-top.css` → `components/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.css`

## 5.2 No-op block files → `blocks/`

Copy the no-op placeholder files to the blocks directory:

```bash
mkdir -p blocks/TARGET_BLOCK_NAME/
```

Copy:
- `back-to-top-noop.js` → `blocks/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.js`
- `back-to-top-noop.css` → `blocks/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.css`

**Important:** The no-op files go to `blocks/` with the standard block name (without the `-noop` suffix).

---

# 6. COPY CSS/SCSS FILES INTO THE TARGET PROJECT

CSS files are handled in Step 5 (both the Web Component CSS and the no-op CSS).

Additionally, the parent block's CSS needs to import the Web Component CSS:

Add to the parent block's CSS file (e.g., `blocks/header/header.css`):

```css
@import url("../../components/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.css");
```

Verify this import doesn't already exist before adding.

---

# 7. REGISTER MODEL / CONFIG FILES

## 7.1 Copy block content model

Copy `_back-to-top.json` to `blocks/TARGET_BLOCK_NAME/_TARGET_BLOCK_NAME.json`.

## 7.2 Register in parent block filter

Open the parent block's JSON model (e.g., `blocks/header/_header.json`) and add `"TARGET_BLOCK_NAME"` to its filter's `components` array.

## 7.3 Register in component model files (if applicable)

If the project uses component model files (`HAS_COMPONENT_MODELS`):

1. **`component-definition.json`** — Add the block definition entry
2. **`component-models.json`** — Add the model fields entry
3. **`component-filters.json`** — Add to the parent block's filter

Use the `model-snippet.json` and `_{BLOCK_NAME}.json` as references for the exact structure.

---

# 8. UPDATE BLOCK INDEX / ROUTER (OPTIONAL)

## 8.1 Update parent block imports

Open the parent block's JS file (e.g., `blocks/header/header.js`) and add:

```javascript
import TARGET_FUNC from '../../components/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.js';
```

## 8.2 Add instantiation logic

Use `header-integration-snippet.js` as a reference to add the instantiation code in the parent block's `decorate()` function:

```javascript
// Append TARGET_BLOCK_NAME button to document if it doesn't exist
const targetBlock = fragment.querySelector('.TARGET_BLOCK_NAME');
if (targetBlock && !document.querySelector('TARGET_BLOCK_NAME')) {
  const instance = new TARGET_FUNC(targetBlock);
  document.body.appendChild(instance);
}
```

Replace all placeholder names with the actual target names.

---

# 9. SANITY CHECKS

After copying and wiring:

| # | Check | How |
|---|-------|-----|
| 1 | No remaining placeholders | Search for `{BLOCK_` in all block/component files |
| 2 | All imports resolve | Verify import paths match actual file locations |
| 3 | Component folder has files | List `components/TARGET_BLOCK_NAME/` |
| 4 | Block folder has no-op files | List `blocks/TARGET_BLOCK_NAME/` |
| 5 | Content model copied | Check `blocks/TARGET_BLOCK_NAME/_TARGET_BLOCK_NAME.json` |
| 6 | Parent block imports component | Verify import statement in parent block JS |
| 7 | Parent block CSS imports component CSS | Verify @import in parent block CSS |
| 8 | Custom element tag matches | Verify `customElements.define()` tag matches CSS selectors |
| 9 | Analytics dependency resolved | `trackNavigationScrollTop` exists or is stubbed |

## 9.1 Resolve external dependencies

### Analytics (OPTIONAL)

`trackNavigationScrollTop` from `scripts/analytics/data-layer.js`

**If not available, stub:**

```javascript
// scripts/analytics/data-layer.js (add export if needed)
export function trackNavigationScrollTop() {}
```

### Icon SVG

The component renders an arrow icon via `window.hlx.codeBasePath + '/icons/arrow-upward.svg'`.

**Ensure this icon exists** in the target project's `icons/` folder, or replace with an equivalent upward arrow SVG.

---

# 10. RUN TESTS (IF NO ISSUES FOUND)

If all sanity checks pass and no critical issues were found during implementation, run the following tests:

## 10.1 Build/Compile Test

Run the project's build command to ensure no compilation errors:

```bash
# Common EDS/AEM build commands (run the appropriate one for this project)
npm run build
# or
npm run lint
# or
aem up
```

If build fails:
- Report the exact error
- Identify which file is causing the issue
- Suggest fixes but do not modify without user confirmation

## 10.2 Development Server Test (Optional)

If the build succeeds, offer to start the development server:

```bash
# Common dev server commands
npm run dev
# or
npm start
# or
aem up
```

Then inform the user:

> Development server started successfully.
> You can now test the block at: [provide URL if known, typically http://localhost:3000]
>
> To test the block:
> 1. Navigate to a page that includes a header with the back-to-top block
> 2. Scroll down past 300px — the button should appear
> 3. Click the button — page should scroll smoothly to top
> 4. Test keyboard: Tab to button, press Enter/Space
> 5. Check browser console for any errors

## 10.3 Linting Test

Run linting to check code quality:

```bash
npm run lint
# or
npm run lint:fix  # to auto-fix issues
```

Report any linting errors or warnings found.

---

# 11. CLEANUP - DELETE TEMPORARY EXTRACTION FOLDER

**ONLY proceed with cleanup if:**
- All tests passed successfully (build, linting)
- Block is confirmed working in the target repository
- User confirms they are ready to clean up

Before deleting, ask the user for confirmation:

> All implementation steps completed successfully!
>
> The block has been integrated into your project at:
> - `components/TARGET_BLOCK_NAME/` (Web Component JS + CSS)
> - `blocks/TARGET_BLOCK_NAME/` (no-op decorator + content model)
> - Parent block updated with import and instantiation
>
> **Ready to clean up?**
> 
> I can now delete the temporary extraction folder:
> - `_block-export/Back-To-Top/`
> 
> This will remove:
> - All extracted files (JS, CSS, JSON, docs)
> - `extraction-information.md`
> - `summary.md`
> - This `implementation.md` file itself
>
> The block will remain fully functional in `components/TARGET_BLOCK_NAME/` and `blocks/TARGET_BLOCK_NAME/`.
>
> **Should I proceed with cleanup?** (yes/no)

Wait for user confirmation.

## 11.1 If User Confirms Cleanup

Delete the entire `_block-export/Back-To-Top/` directory:

```bash
rm -rf _block-export/Back-To-Top
```

**After deletion, inform the user:**

> Cleanup completed successfully.
>
> **Deleted:**
> - `_block-export/Back-To-Top/` (entire folder)
>
> **Preserved:**
> - `components/TARGET_BLOCK_NAME/` — Web Component is live here
> - `blocks/TARGET_BLOCK_NAME/` — Block decorator and content model
> - Parent block files updated
>
> The block is now fully integrated and the temporary files have been removed.

## 11.2 If User Declines Cleanup

> No problem! The temporary folder will remain at:
> - `_block-export/Back-To-Top/`
>
> You can delete it manually later when ready, or keep it for reference.
> The block is already fully functional.

---

# 12. FINAL SUMMARY TO USER

After all steps (including tests and cleanup), respond with something like:

> Implementation completed.  
>
> **Block Names:**  
> - Original source name: back-to-top
> - Implemented as: TARGET_BLOCK_NAME  
>
> **Architecture:**
> - Type: Web Component (`<TARGET_BLOCK_NAME>` custom element)
> - Parent block: PARENT_BLOCK_JS_PATH
>
> **Naming Replacements:**
> - Total placeholders replaced: [count]
> - File names updated: [list]
> - Custom element tag: `<TARGET_BLOCK_NAME>`
> - JS class: `TargetBlockName extends HTMLElement`
>
> **Files Created:**  
> - Web Component: `components/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.js`
> - Web Component CSS: `components/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.css`
> - Block decorator: `blocks/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.js` (no-op)
> - Block CSS: `blocks/TARGET_BLOCK_NAME/TARGET_BLOCK_NAME.css` (empty)
> - Content model: `blocks/TARGET_BLOCK_NAME/_TARGET_BLOCK_NAME.json`
>
> **Files Modified:**  
> - Parent block JS: added import + instantiation
> - Parent block CSS: added @import for component CSS
> - Component model files (if applicable)
>
> **Test Results:**  
> - Build: ✅ Passed / ❌ Failed (with details)
> - Linting: ✅ Passed / ⚠️ Warnings (with details)
> - Dev Server: ✅ Started / ⏭️ Skipped
>
> **Cleanup:**  
> - Temporary extraction folder: ✅ Deleted / ⏭️ Kept (user choice)
>
> **Potential TODOs:**  
> - Verify the back-to-top button appears after scrolling 300px
> - Test smooth scroll behavior
> - Ensure arrow-upward.svg icon exists in `icons/` folder
> - Verify keyboard accessibility (Enter/Space)
> - Test `prefers-reduced-motion` behavior
> - Check analytics events fire (if analytics is enabled)
>
> Let me know if you want me to help test or debug any issues.

If any step cannot proceed → stop, explain, ask for clarification.

---
