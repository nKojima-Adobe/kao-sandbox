/**
 * Header Start marker block.
 * No-op decorator — this block is a hidden sentinel used by the header
 * to determine when to show itself on scroll.
 */
export default function decorate() {
  // intentionally empty
}
