/**
 * Announcement block decorator.
 * @param {HTMLElement} block The announcement block element
 */
export default function decorate(block) {
  // Content is authored via richtext and rendered as-is with border styling from CSS
}
